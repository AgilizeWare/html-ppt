<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Regulação de Sinistro · IA</title>
<style>
  :root{
    --bg:#08080b; --surface:#121217; --surface-2:#1a1a21; --surface-3:#212129;
    --border:#26262f; --border-soft:#1e1e25;
    --text:#f4f4f7; --text-muted:#8c8c97; --text-dim:#5d5d67;
    --accent:#ec2a8a; --accent-2:#d6248a; --accent-soft:rgba(236,42,138,.12); --accent-border:rgba(236,42,138,.35);
    --green:#34d399; --green-soft:rgba(52,211,153,.12);
    --amber:#fbbf24; --amber-soft:rgba(251,191,36,.12);
    --red:#f87171; --red-soft:rgba(248,113,113,.12);
    --blue:#60a5fa; --blue-soft:rgba(96,165,250,.12);
    --radius:16px; --radius-sm:11px;
    --font:'Inter',-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,sans-serif;
  }
  *{box-sizing:border-box;margin:0;padding:0}
  body{font-family:var(--font);
    background:radial-gradient(1200px 600px at 80% -10%,rgba(236,42,138,.07),transparent 60%),radial-gradient(900px 500px at 0% 110%,rgba(96,165,250,.05),transparent 55%),var(--bg);
    color:var(--text);min-height:100vh;-webkit-font-smoothing:antialiased;font-size:14px;line-height:1.5}
  .app{max-width:1180px;margin:0 auto;padding:22px 26px 80px}

  .topbar{display:flex;align-items:center;justify-content:space-between;margin-bottom:30px}
  .brand{display:flex;align-items:center;gap:11px}
  .brand .mark{width:36px;height:36px;border-radius:10px;background:linear-gradient(135deg,var(--accent),var(--accent-2));display:grid;place-items:center;font-size:18px;box-shadow:0 6px 20px rgba(236,42,138,.35)}
  .brand .t{font-weight:700;font-size:15px;letter-spacing:-.2px}
  .brand .s{font-size:11.5px;color:var(--text-dim);font-weight:500}
  .pill-user{display:flex;align-items:center;gap:8px;font-size:12.5px;color:var(--text-muted);background:var(--surface);border:1px solid var(--border-soft);padding:6px 12px;border-radius:999px}
  .avatar{width:22px;height:22px;border-radius:50%;background:linear-gradient(135deg,#7c3aed,#ec2a8a);display:grid;place-items:center;font-size:10px;font-weight:700;color:#fff}

  .btn{display:inline-flex;align-items:center;gap:8px;font-family:inherit;font-size:13.5px;font-weight:600;border:none;border-radius:11px;padding:11px 18px;cursor:pointer;transition:.16s;white-space:nowrap}
  .btn-primary{background:linear-gradient(135deg,var(--accent),var(--accent-2));color:#fff;box-shadow:0 6px 18px rgba(236,42,138,.32)}
  .btn-primary:hover{filter:brightness(1.08);transform:translateY(-1px)}
  .btn-ghost{background:var(--surface);color:var(--text);border:1px solid var(--border)}
  .btn-ghost:hover{background:var(--surface-2);border-color:#34343f}
  .btn-soft{background:var(--accent-soft);color:var(--accent);border:1px solid var(--accent-border)}
  .btn-soft:hover{background:rgba(236,42,138,.18)}
  .btn-warn{background:var(--amber-soft);color:var(--amber);border:1px solid rgba(251,191,36,.35)}
  .btn-warn:hover{background:rgba(251,191,36,.2)}
  .btn-link{background:none;border:none;color:var(--text-muted);cursor:pointer;font-size:13.5px;font-weight:600;font-family:inherit;padding:11px 10px}
  .btn-link:hover{color:var(--text)}
  .btn:disabled{opacity:.45;cursor:not-allowed;transform:none;filter:none}
  .btn.sm{padding:8px 13px;font-size:12.5px}

  .view{display:none;animation:fade .35s ease}
  .view.active{display:block}
  @keyframes fade{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:none}}

  .page-title{display:flex;align-items:center;gap:11px;font-size:21px;font-weight:700;letter-spacing:-.4px}
  .page-title .ic{width:30px;height:30px;border-radius:9px;background:var(--accent-soft);display:grid;place-items:center;font-size:16px}
  .page-sub{color:var(--text-muted);font-size:13.5px;margin:7px 0 0;max-width:660px}

  .card{background:var(--surface);border:1px solid var(--border-soft);border-radius:var(--radius);box-shadow:0 1px 0 rgba(255,255,255,.02) inset,0 18px 40px -28px rgba(0,0,0,.9)}
  .card-pad{padding:22px}
  .section-label{font-size:13px;font-weight:700;color:var(--text);margin-bottom:3px}
  .section-hint{font-size:12px;color:var(--text-dim);margin-bottom:16px}
  .divider{height:1px;background:var(--border-soft);margin:20px 0}

  .grid{display:grid;gap:16px}.g2{grid-template-columns:1fr 1fr}.g3{grid-template-columns:1fr 1fr 1fr}
  label.fl{display:block;font-size:12px;color:var(--text-muted);margin-bottom:7px;font-weight:600}
  .inp,select.inp,textarea.inp{width:100%;background:var(--surface-2);border:1px solid var(--border);border-radius:var(--radius-sm);color:var(--text);font-family:inherit;font-size:13.5px;padding:11px 13px;transition:.15s;outline:none}
  .inp:focus,select.inp:focus,textarea.inp:focus{border-color:var(--accent-border);box-shadow:0 0 0 3px var(--accent-soft)}
  .inp::placeholder,textarea.inp::placeholder{color:var(--text-dim)}
  textarea.inp{resize:vertical;min-height:64px}
  .inp[readonly]{color:var(--text-muted);cursor:default}

  .chips{display:flex;gap:8px;flex-wrap:wrap;margin-top:9px}
  .chip-btn{font-size:11.5px;font-weight:600;color:var(--text-muted);background:var(--surface-2);border:1px solid var(--border);border-radius:999px;padding:6px 12px;cursor:pointer;transition:.13s}
  .chip-btn:hover{border-color:var(--accent-border);color:var(--text)}

  .home-grid{display:grid;grid-template-columns:1fr 1fr;gap:18px;margin-top:26px}
  .choice{cursor:pointer;transition:.18s;position:relative;overflow:hidden}
  .choice:hover{transform:translateY(-3px);border-color:var(--accent-border)}
  .choice .glow{position:absolute;inset:0;opacity:0;transition:.18s;background:radial-gradient(400px 200px at 50% 0%,var(--accent-soft),transparent 70%)}
  .choice:hover .glow{opacity:1}
  .choice .ico{width:46px;height:46px;border-radius:13px;display:grid;place-items:center;font-size:22px;margin-bottom:16px}
  .choice h3{font-size:16px;font-weight:700;letter-spacing:-.2px}
  .choice p{font-size:13px;color:var(--text-muted);margin-top:6px}
  .choice .cta{margin-top:18px;display:flex;align-items:center;gap:7px;font-size:13px;font-weight:600;color:var(--accent)}

  .recent{margin-top:30px}
  .recent h4{font-size:12px;text-transform:uppercase;letter-spacing:.6px;color:var(--text-dim);margin-bottom:12px;font-weight:700}
  .case-row{display:flex;align-items:center;gap:14px;padding:13px 16px;border:1px solid var(--border-soft);border-radius:12px;background:var(--surface);cursor:pointer;transition:.15s;margin-bottom:9px}
  .case-row:hover{background:var(--surface-2);border-color:var(--border)}
  .case-row .id{font-weight:600;font-size:13.5px}
  .case-row .meta{font-size:12px;color:var(--text-dim)}
  .case-row .spacer{flex:1}

  .pill{display:inline-flex;align-items:center;gap:6px;font-size:11.5px;font-weight:600;padding:4px 11px;border-radius:999px;white-space:nowrap}
  .pill .dot{width:6px;height:6px;border-radius:50%}
  .pill.amber{background:var(--amber-soft);color:var(--amber)} .pill.amber .dot{background:var(--amber)}
  .pill.green{background:var(--green-soft);color:var(--green)} .pill.green .dot{background:var(--green)}
  .pill.blue{background:var(--blue-soft);color:var(--blue)} .pill.blue .dot{background:var(--blue)}
  .pill.red{background:var(--red-soft);color:var(--red)} .pill.red .dot{background:var(--red)}
  .pill.gray{background:var(--surface-3);color:var(--text-muted)} .pill.gray .dot{background:var(--text-dim)}

  .type-toggle{display:flex;gap:8px;margin-bottom:16px}
  .type-btn{flex:1;border:1px solid var(--border);background:var(--surface-2);border-radius:11px;padding:12px;cursor:pointer;transition:.15s;text-align:left}
  .type-btn.active{border-color:var(--accent-border);background:var(--accent-soft)}
  .type-btn:not(.locked):hover{border-color:#34343f}
  .type-btn .tb-t{font-weight:700;font-size:13.5px;display:flex;align-items:center;gap:7px}
  .type-btn .tb-s{font-size:11.5px;color:var(--text-dim);margin-top:3px}
  .type-btn.locked{cursor:not-allowed;opacity:.5}

  .prod-results{margin-top:10px;border:1px solid var(--border);border-radius:12px;overflow:hidden;display:none;max-height:340px;overflow-y:auto}
  .prod-results.show{display:block;animation:fade .25s}
  .prod-head{font-size:11px;text-transform:uppercase;letter-spacing:.5px;color:var(--text-dim);font-weight:700;padding:10px 14px;background:var(--surface-2);border-bottom:1px solid var(--border-soft)}
  .prod-item{display:flex;align-items:center;gap:12px;padding:12px 14px;cursor:pointer;border-bottom:1px solid var(--border-soft);transition:.13s}
  .prod-item:last-child{border-bottom:none}
  .prod-item:hover{background:var(--surface-2)}
  .prod-item.sel{background:var(--accent-soft)}
  .prod-item .pi-ic{width:32px;height:32px;border-radius:9px;background:var(--surface-3);display:grid;place-items:center;font-size:15px;flex-shrink:0}
  .prod-item .pi-name{font-weight:600;font-size:13.5px}
  .prod-item .pi-meta{font-size:11.5px;color:var(--text-dim);margin-top:2px}
  .prod-item .check{margin-left:auto;color:var(--accent);opacity:0;font-size:16px}
  .prod-item.sel .check{opacity:1}
  .badge-yr{font-size:10px;font-weight:700;padding:2px 7px;border-radius:6px;background:var(--surface-3);color:var(--text-muted);margin-left:8px}
  .badge-yr.match{background:var(--green-soft);color:var(--green)}

  .case-head{display:flex;align-items:flex-start;justify-content:space-between;gap:16px;margin-bottom:22px;flex-wrap:wrap}
  .case-head .meta-line{display:flex;align-items:center;gap:10px;flex-wrap:wrap;margin-top:8px}
  .case-head .chip{display:inline-flex;align-items:center;gap:7px;font-size:12.5px;color:var(--text-muted);background:var(--surface);border:1px solid var(--border-soft);padding:5px 11px;border-radius:9px}
  .case-head .chip b{color:var(--text);font-weight:600}
  .ws{display:grid;grid-template-columns:1.55fr 1fr;gap:18px;align-items:start}

  /* eligibility */
  .elig{display:flex;gap:13px;border-radius:14px;padding:15px 16px;margin-bottom:18px}
  .elig.ok{background:var(--green-soft);border:1px solid rgba(52,211,153,.28)}
  .elig.no{background:var(--red-soft);border:1px solid rgba(248,113,113,.3)}
  .elig .eic{font-size:20px;flex-shrink:0}
  .elig .et{font-size:13.5px;font-weight:700}
  .elig.ok .et{color:var(--green)} .elig.no .et{color:var(--red)}
  .elig .es{font-size:12.5px;color:var(--text-muted);margin-top:4px;line-height:1.5}
  .elig .es b{color:var(--text)}
  .elig-actions{margin-top:12px;display:flex;gap:8px;flex-wrap:wrap}

  .lock-cl{text-align:center;padding:26px 18px;color:var(--text-muted)}
  .lock-cl .lic{font-size:30px;margin-bottom:10px;opacity:.6}

  .panel-h{display:flex;align-items:center;justify-content:space-between;margin-bottom:4px}
  .panel-h h3{font-size:15px;font-weight:700;letter-spacing:-.2px;display:flex;align-items:center;gap:8px}
  .panel-h .count{font-size:12px;color:var(--text-dim);font-weight:600}
  .progress-wrap{margin:14px 0 4px}
  .progress-bar{height:7px;background:var(--surface-3);border-radius:999px;overflow:hidden}
  .progress-fill{height:100%;width:0%;background:linear-gradient(90deg,var(--accent),#f76fb3);border-radius:999px;transition:width .5s cubic-bezier(.2,.8,.2,1)}
  .progress-txt{font-size:11.5px;color:var(--text-muted);margin-top:7px;font-weight:600}

  .ot-banner{display:flex;gap:11px;background:var(--amber-soft);border:1px solid rgba(251,191,36,.28);border-radius:12px;padding:12px 14px;margin:16px 0 6px}
  .ot-banner .ic{font-size:16px;flex-shrink:0}
  .ot-banner .txt{font-size:12.5px;color:#f0d089;line-height:1.45}
  .ot-banner b{color:var(--amber)}

  .checklist{list-style:none;margin-top:8px}
  .ci{display:flex;gap:13px;padding:14px 4px;border-bottom:1px solid var(--border-soft)}
  .ci:last-child{border-bottom:none}
  .ci .stat{width:24px;height:24px;border-radius:50%;flex-shrink:0;display:grid;place-items:center;font-size:13px;margin-top:1px;transition:.2s;font-weight:700}
  .ci .stat.pendente{border:2px dashed var(--border);color:transparent}
  .ci .stat.analise{border:2px solid var(--amber);color:var(--amber)}
  .ci .stat.validado{background:var(--green);color:#06281c}
  .ci .stat.diverg{background:var(--amber);color:#3a2503}
  .ci .body{flex:1;min-width:0}
  .ci .nm{font-size:13.5px;font-weight:600}
  .ci .why{display:flex;align-items:center;gap:8px;margin-top:6px;flex-wrap:wrap}
  .tag{font-size:10.5px;font-weight:600;padding:3px 8px;border-radius:7px;display:inline-flex;align-items:center;gap:5px}
  .tag.cg{background:var(--surface-3);color:var(--text-muted)}
  .tag.ot{background:var(--amber-soft);color:var(--amber);border:1px solid rgba(251,191,36,.25)}
  .ci .docref{display:flex;align-items:center;gap:6px;font-size:11.5px;color:var(--green);margin-top:8px;font-weight:600}
  .ci .docref a{color:var(--green);text-decoration:none;cursor:pointer}
  .ci .docref a:hover{text-decoration:underline}
  .ci .ressalva{font-size:11px;color:var(--amber);margin-top:6px;font-weight:600}
  .spin{width:13px;height:13px;border:2px solid var(--amber);border-top-color:transparent;border-radius:50%;animation:sp .7s linear infinite}
  @keyframes sp{to{transform:rotate(360deg)}}

  .diverg-box{margin-top:10px;background:var(--amber-soft);border:1px solid rgba(251,191,36,.28);border-radius:10px;padding:11px 12px}
  .diverg-box .db-t{font-size:11.5px;font-weight:700;color:var(--amber);display:flex;align-items:center;gap:6px}
  .cmp{display:flex;gap:10px;margin-top:9px}
  .cmp .col{flex:1;background:rgba(0,0,0,.18);border-radius:8px;padding:8px 10px}
  .cmp .lb{font-size:10px;text-transform:uppercase;letter-spacing:.4px;color:var(--text-dim);font-weight:700}
  .cmp .vl{font-size:12px;font-weight:600;margin-top:3px}
  .cmp .vl.bad{color:var(--red)}
  .mini-btns{display:flex;gap:8px;margin-top:11px}
  .mini-btn{font-size:11.5px;font-weight:600;border-radius:8px;padding:7px 12px;cursor:pointer;border:1px solid var(--border);background:var(--surface-2);color:var(--text-muted);transition:.13s}
  .mini-btn:hover{color:var(--text);border-color:#34343f}
  .mini-btn.ok{background:var(--green-soft);color:var(--green);border-color:rgba(52,211,153,.3)}
  .mini-btn.re{background:var(--surface-2)}

  .dropzone{border:1.5px dashed var(--border);border-radius:13px;padding:22px 16px;text-align:center;cursor:pointer;transition:.18s;background:var(--surface-2)}
  .dropzone:hover{border-color:var(--accent-border);background:var(--accent-soft)}
  .dropzone .dz-ic{font-size:24px;margin-bottom:8px}
  .dropzone .dz-t{font-size:13px;font-weight:600}
  .dropzone .dz-s{font-size:11.5px;color:var(--text-dim);margin-top:4px;line-height:1.5}
  .dz-extra{text-align:center;margin-top:10px}
  .dz-extra a{font-size:12px;color:var(--accent);font-weight:600;cursor:pointer}
  .dz-extra a:hover{text-decoration:underline}
  .doc-list{list-style:none;margin-top:14px}
  .doc{display:flex;align-items:center;gap:11px;padding:11px 12px;border:1px solid var(--border-soft);border-radius:11px;margin-bottom:9px;background:var(--surface-2)}
  .doc .fic{width:34px;height:34px;border-radius:9px;background:var(--surface-3);display:grid;place-items:center;font-size:15px;flex-shrink:0}
  .doc .dn{font-size:12.5px;font-weight:600;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
  .doc .dm{font-size:11px;color:var(--text-dim);margin-top:2px}
  .doc .dl{margin-left:auto;flex-shrink:0;width:30px;height:30px;border-radius:8px;background:var(--surface-3);border:none;color:var(--text-muted);cursor:pointer;display:grid;place-items:center;transition:.13s}
  .doc .dl:hover{background:var(--accent-soft);color:var(--accent)}
  .doc.flag{border-color:rgba(251,191,36,.3)}
  .doc .warn{color:var(--amber);font-size:13px;margin-left:auto;margin-right:4px}
  .empty{text-align:center;color:var(--text-dim);font-size:12px;padding:18px 0}
  .panel-gap{height:18px}

  .toasts{position:fixed;bottom:24px;right:24px;display:flex;flex-direction:column;gap:10px;z-index:90}
  .toast{display:flex;gap:11px;align-items:flex-start;background:var(--surface-2);border:1px solid var(--border);border-radius:13px;padding:13px 15px;min-width:300px;max-width:380px;box-shadow:0 18px 40px -16px rgba(0,0,0,.8);animation:slideIn .3s}
  .toast .ti{font-size:17px;flex-shrink:0}
  .toast .tt{font-size:13px;font-weight:600}
  .toast .ts{font-size:11.5px;color:var(--text-muted);margin-top:3px;line-height:1.4}
  @keyframes slideIn{from{opacity:0;transform:translateX(40px)}to{opacity:1;transform:none}}
  @keyframes slideOut{to{opacity:0;transform:translateX(40px)}}

  .footer-note{text-align:center;color:var(--text-dim);font-size:11px;margin-top:34px}
  @media(max-width:880px){.ws{grid-template-columns:1fr}.home-grid{grid-template-columns:1fr}.g2,.g3{grid-template-columns:1fr}}
</style>
</head>
<body>
<div class="app">

  <div class="topbar">
    <div class="brand">
      <div class="mark">◇</div>
      <div><div class="t">Regulação de Sinistro</div><div class="s">Plataforma agêntica · Caixa Vida e Previdência</div></div>
    </div>
    <div class="pill-user"><span class="avatar">PC</span> Pedro · Regulador</div>
  </div>

  <!-- HOME -->
  <div id="view-home" class="view active">
    <div class="page-title"><span class="ic">📂</span> Central de Regulação</div>
    <p class="page-sub">Abra um novo sinistro ou consulte um caso. Cada caso passa por elegibilidade, gera o checklist a partir das Condições Gerais do produto e faz a validação cruzada dos documentos.</p>
    <div class="home-grid">
      <div class="card card-pad choice" onclick="goForm()">
        <div class="glow"></div><div class="ico" style="background:var(--accent-soft);color:var(--accent)">＋</div>
        <h3>Cadastrar novo sinistro</h3>
        <p>Informe os dados, escolha o produto pelo ano de adesão e dispare a análise.</p>
        <div class="cta">Abrir cadastro →</div>
      </div>
      <div class="card card-pad choice" onclick="openConsulta()">
        <div class="glow"></div><div class="ico" style="background:var(--blue-soft);color:var(--blue)">🔎</div>
        <h3>Consultar sinistro</h3>
        <p>Busque um caso existente pelo ID e acompanhe checklist e validações.</p>
        <div class="cta" style="color:var(--blue)">Buscar por ID →</div>
      </div>
    </div>
    <div class="recent">
      <h4>Casos recentes</h4>
      <div class="case-row" onclick="openCase('default')">
        <span class="pill amber"><span class="dot"></span>Divergências a resolver</span>
        <div><div class="id">sinistro-2026-04817</div><div class="meta">Vida Mulher (adesão 2020 · +30 Mil) · Morte natural</div></div>
        <div class="spacer"></div><div class="meta">3 docs · 1 divergência (RG do segurado)</div><span style="color:var(--text-dim)">→</span>
      </div>
      <div class="case-row" onclick="openCase('herdeiros')">
        <span class="pill amber"><span class="dot"></span>Divergências a resolver</span>
        <div><div class="id">sinistro-2026-04825</div><div class="meta">Vida Mulher · Morte natural · sem beneficiário indicado (2 herdeiros)</div></div>
        <div class="spacer"></div><div class="meta">2 docs · 1 divergência (RG do filho)</div><span style="color:var(--text-dim)">→</span>
      </div>
      <div class="case-row" onclick="openCase('naoelegivel')">
        <span class="pill red"><span class="dot"></span>Não elegível</span>
        <div><div class="id">sinistro-2026-04830</div><div class="meta">Vida Mulher · Suicídio dentro da carência</div></div>
        <div class="spacer"></div><div class="meta">checklist não gerado</div><span style="color:var(--text-dim)">→</span>
      </div>
    </div>
  </div>

  <!-- CONSULTA -->
  <div id="view-consulta" class="view">
    <button class="btn-link" onclick="goHome()">← Voltar</button>
    <div style="max-width:560px;margin:18px auto 0">
      <div class="page-title"><span class="ic">🔎</span> Consultar sinistro</div>
      <p class="page-sub">Digite o ID do sinistro para abrir o caso.</p>
      <div class="card card-pad" style="margin-top:22px">
        <label class="fl">ID do sinistro</label>
        <div style="display:flex;gap:10px">
          <input class="inp" id="consultaId" placeholder="Ex.: sinistro-2026-04817" value="sinistro-2026-04817">
          <button class="btn btn-primary" onclick="openCase()">Abrir caso</button>
        </div>
        <p style="font-size:11.5px;color:var(--text-dim);margin-top:12px">Dica: use <b style="color:var(--text-muted)">sinistro-2026-04817</b> para ver um caso de exemplo.</p>
      </div>
    </div>
  </div>

  <!-- FORM -->
  <div id="view-form" class="view">
    <button class="btn-link" onclick="goHome()">← Voltar</button>
    <div style="margin:14px 0 22px">
      <div class="page-title"><span class="ic">📝</span> Abrir Regulação de Sinistro</div>
      <p class="page-sub">Informe os dados do sinistro e selecione o produto. O sistema verifica elegibilidade e, sendo coberto, monta o checklist a partir das Condições Gerais.</p>
    </div>

    <div class="card card-pad" style="max-width:760px">
      <div class="section-label">Sinistro</div>
      <div class="section-hint">Identificação e motivo do evento reportado.</div>
      <div class="grid g2">
        <div><label class="fl">ID do sinistro</label><input class="inp" id="f-id" placeholder="sinistro-..." value="sinistro-2026-04817"></div>
        <div><label class="fl">Data do evento</label><input class="inp" id="f-data" value="22/05/2026"></div>
      </div>
      <div style="margin-top:16px">
        <label class="fl">Motivo reportado</label>
        <textarea class="inp" id="f-motivo" placeholder="Ex.: Morte natural do segurado titular">Morte natural do segurado titular</textarea>
        <div class="chips">
          <span class="chip-btn" onclick="setMotivo('Morte natural do segurado titular')">Morte natural ✓</span>
          <span class="chip-btn" onclick="setMotivo('Suicídio dentro do período de carência')">Suicídio na carência ✕</span>
          <span class="chip-btn" onclick="setMotivo('Doença preexistente não declarada na contratação')">Doença preexistente ✕</span>
        </div>
      </div>

      <div class="divider"></div>

      <div class="section-label">Dados do sinistro · adesão</div>
      <div class="section-hint">Usados para localizar a apólice e a versão correta da Condição Geral.</div>
      <div class="grid g2">
        <div><label class="fl">Ano de adesão</label>
          <select class="inp" id="f-ano" onchange="if(prodOpen)queryProd()"><option>2020</option><option>2022</option><option>2024</option></select></div>
        <div><label class="fl">Mês de adesão</label><input class="inp" id="f-mes" value="Dezembro"></div>
      </div>

      <div class="divider"></div>

      <div class="section-label">Produto</div>
      <div class="section-hint">O mesmo subproduto tem várias versões — a versão certa depende do ano de adesão e da periodicidade/capital.</div>
      <div class="type-toggle">
        <div class="type-btn active" id="tb-vida"><div class="tb-t">💗 Vida</div><div class="tb-s">Morte, invalidez, doença grave</div></div>
        <div class="type-btn locked" title="Em breve"><div class="tb-t">🔒 Prestamista</div><div class="tb-s">Indisponível nesta versão</div></div>
      </div>

      <label class="fl">Buscar produto no banco</label>
      <div style="display:flex;gap:10px">
        <input class="inp" id="prodSearch" placeholder="Ex.: Vida Mulher" value="Vida Mulher" oninput="if(prodOpen)queryProd()">
        <button class="btn btn-soft" onclick="queryProd()">⚙ Consultar produtos</button>
      </div>
      <div class="prod-results" id="prodResults"></div>

      <div id="prodSelected" style="display:none;margin-top:16px">
        <div class="grid g2">
          <div><label class="fl">Versão selecionada</label><input class="inp" id="f-prod" readonly></div>
          <div><label class="fl">Periodicidade / capital</label><input class="inp" id="f-period" readonly></div>
        </div>
        <p style="font-size:11.5px;color:var(--text-dim);margin-top:10px">📑 CG vinculada: <b id="f-cg" style="color:var(--text-muted)"></b></p>
      </div>

      <div style="display:flex;justify-content:flex-end;gap:6px;margin-top:24px">
        <button class="btn-link" onclick="goHome()">Cancelar</button>
        <button class="btn btn-primary" id="btnDisparar" onclick="dispararAnalise()" disabled>✦ Disparar Análise</button>
      </div>
    </div>
  </div>

  <!-- CASE -->
  <div id="view-case" class="view">
    <button class="btn-link" onclick="goHome()">← Voltar</button>
    <div class="case-head" style="margin-top:14px">
      <div>
        <div class="page-title"><span class="ic">🗂️</span> <span id="c-id">sinistro-...</span></div>
        <div class="meta-line">
          <span class="chip">Produto: <b id="c-prod">—</b></span>
          <span class="chip">Motivo: <b id="c-motivo">—</b></span>
          <span class="chip">Adesão: <b id="c-adesao">—</b></span>
        </div>
      </div>
      <div id="c-status"></div>
    </div>

    <div class="ws">
      <div class="card card-pad">
        <div id="eligBox"></div>
        <div id="checklistArea">
          <div class="panel-h"><h3>✓ Checklist de documentos</h3><span class="count" id="cl-count">0/0</span></div>
          <div class="progress-wrap"><div class="progress-bar"><div class="progress-fill" id="cl-fill"></div></div>
            <div class="progress-txt" id="cl-txt">0 de 0 documentos validados</div></div>
          <div id="ot-banner"></div>
          <ul class="checklist" id="checklist"></ul>
        </div>
      </div>

      <div>
        <div class="card card-pad">
          <div class="panel-h"><h3>📎 Documentos do Sinistro</h3></div>
          <p class="section-hint" style="margin:6px 0 14px">Suba qualquer documento — a IA identifica o tipo, faz a validação cruzada com o cadastro e dá o check sozinha.</p>
          <div class="dropzone" onclick="receberDoc()">
            <div class="dz-ic">⬆️</div>
            <div class="dz-t">Soltar ou clicar para enviar</div>
            <div class="dz-s">PDF, Word, TXT, imagem, áudio ou vídeo · um arquivo pode conter vários documentos — a IA separa, classifica e dá o check sozinha.</div>
          </div>
          <ul class="doc-list" id="docList"><li class="empty">Nenhum documento recebido ainda.</li></ul>
        </div>

        <div class="panel-gap"></div>

        <div class="card card-pad">
          <div class="panel-h"><h3>📑 Condições Gerais</h3></div>
          <p class="section-hint" style="margin:6px 0 14px">Documentos normativos da versão do produto usada nesta regulação.</p>
          <ul class="doc-list" id="cgList"></ul>
        </div>
      </div>
    </div>
  </div>

  <div class="footer-note">Protótipo de fluxo · Regulação de Sinistro com IA · dados fictícios para demonstração</div>
</div>

<div class="toasts" id="toasts"></div>

<script>
/* ===== banco de produtos (simulado, espelha estrutura da CVP) ===== */
const PRODUTOS=[
  {id:2023,sub:"Vida Mulher",ano:2020,mes:"Dezembro",periodicidade:"Mensal - Capital até 30 Mil",cg:"VIDA-MULHER--30-mensal-2020-12.pdf",reg:"Regulamento-CVP-Mulher.pdf",maior30:false},
  {id:2024,sub:"Vida Mulher",ano:2020,mes:"Dezembro",periodicidade:"Mensal - Capital Maior 30 Mil",cg:"VIDA-MULHER-+30-mensal-2020-12.pdf",reg:"Regulamento-CVP-Mulher.pdf",maior30:true},
  {id:2204,sub:"Vida Mulher",ano:2022,mes:"Setembro",periodicidade:"Mensal, Antecipado e Único",cg:"Condicoes-Gerais-Vida-Mulher-Setembro-2022-Mensal-Antecipado-Unico.pdf",reg:"Regulamento-XS2-Mulher.pdf",maior30:false},
  {id:5101,sub:"Vida Mulher",ano:2024,mes:"Setembro",periodicidade:"Mensal, Antecipado e Único",cg:"Condicoes-Gerais-Vida-Mulher-Setembro-2024-Mensal-Antecipado-Unico.pdf",reg:"regulamento-promocao-comercial-cvp-mulher.pdf",maior30:false},
  {id:3110,sub:"Vida Sênior",ano:2024,mes:"Janeiro",periodicidade:"Mensal - Capital 20 Mil",cg:"CG-Vida-Senior-2024.pdf",reg:"Reg-Vida-Senior.pdf",maior30:false},
  {id:3111,sub:"Vida Premiável",ano:2023,mes:"Março",periodicidade:"Mensal - Capital 50 Mil",cg:"CG-Vida-Premiavel-2023.pdf",reg:"Reg-Vida-Premiavel.pdf",maior30:true},
];
let selProd=null, prodOpen=false;

/* cadastro do sinistro (referência p/ validação cruzada) */
const CADASTRO={
  segurado:{nome:"João Carlos de Souza", cpf:"123.456.789-00"},
  // beneficiários nomeados na apólice (cada um vira itens próprios no checklist)
  beneficiarios:[{nome:"Maria Souza Lima"}],
  // herdeiros legais quando não há beneficiário indicado (cada filho gera seu próprio RG)
  herdeiros:[
    {nome:"Carlos Eduardo de Souza Lima", rotulo:"filho"},
    {nome:"Ana Paula de Souza Lima",      rotulo:"filha"}
  ]
};
const primeiro=n=>n.split(' ')[0];
const slug=n=>n.toLowerCase().normalize('NFD').replace(/[̀-ͯ]/g,'').replace(/[^a-z]+/g,'_').replace(/^_|_$/g,'');

/* ===== checklist =====
   Aviso de sinistro NÃO entra: a abertura do caso já é o aviso.
   Documentos por pessoa (beneficiário/herdeiro) são gerados um a um. */
function buildChecklist(opts){
  const maior30=opts&&opts.maior30, herdeiros=opts&&opts.herdeiros;
  const items=[
    {nm:"Certidão de Óbito",origem:{t:'cg',ref:'CG · Cláusula 11.2'},file:"CertidaoObito.pdf"},
    {nm:"Documento de identificação do segurado (RG/CPF)",origem:{t:'cg',ref:'CG · Cláusula 11.3'},
       file:"RG_CPF_segurado.pdf", div:{doc:"João C. de Souza Filho",cad:CADASTRO.segurado.nome}},
  ];

  if(herdeiros){
    items.push({nm:"Formal de Partilha ou Alvará Judicial",origem:{t:'ot',ref:'OT-2024-11 · herdeiros legais'},file:"FormalPartilha.pdf"});
    CADASTRO.herdeiros.forEach((h,k)=>{
      items.push({nm:"RG do herdeiro — "+h.nome,sub:h.rotulo+" do segurado",pessoa:h.nome,
        origem:{t:'ot',ref:'OT-2024-11 · herdeiros legais'}, file:"RG_"+slug(h.nome)+".pdf",
        div: k===0 ? {doc:primeiro(h.nome)+" E. Souza",cad:h.nome} : null});
      items.push({nm:"Comprovante de residência — "+primeiro(h.nome),sub:h.rotulo+" do segurado",pessoa:h.nome,
        origem:{t:'cg',ref:'CG · Cláusula 11.4'}, file:"CompResidencia_"+slug(h.nome)+".pdf"});
    });
  } else {
    CADASTRO.beneficiarios.forEach(b=>{
      items.push({nm:"RG do beneficiário — "+b.nome,sub:"beneficiário nomeado",pessoa:b.nome,
        origem:{t:'cg',ref:'CG · Cláusula 11.3'}, file:"RG_"+slug(b.nome)+".pdf",
        div:{doc:primeiro(b.nome)+" S. Lima",cad:b.nome}});
      items.push({nm:"Comprovante de residência — "+primeiro(b.nome),sub:"beneficiário nomeado",pessoa:b.nome,
        origem:{t:'cg',ref:'CG · Cláusula 11.4'}, file:"CompResidencia_"+slug(b.nome)+".pdf"});
    });
  }

  items.push({nm:"Declaração Médica da causa da morte (DO)",origem:{t:'cg',ref:'CG · Cláusula 11.5'},file:"DeclaracaoObito_DO.txt"});
  items.push({nm:"Comprovante de pagamento do prêmio",origem:{t:'cg',ref:'CG · Cláusula 6.2'},file:"ComprovantePremio.pdf"});
  if(maior30) items.push({nm:"Termo de Quitação e Outorga (firma reconhecida)",origem:{t:'ot',ref:'OT-2025-07'},file:"TermoQuitacao_firmado.pdf"});
  return items.map((b)=>({...b,st:'pendente',doc:null,valid:null}));
}

let CL=[], received=[], caseData={}, eligState={ok:true}, overridden=false;

/* ===== nav ===== */
function show(id){document.querySelectorAll('.view').forEach(v=>v.classList.remove('active'));document.getElementById(id).classList.add('active');window.scrollTo({top:0,behavior:'smooth'});}
function goHome(){show('view-home')} function goForm(){show('view-form')} function openConsulta(){show('view-consulta')}
function setMotivo(m){document.getElementById('f-motivo').value=m}

/* ===== produto ===== */
function queryProd(){
  const term=document.getElementById('prodSearch').value.trim();
  const ano=parseInt(document.getElementById('f-ano').value,10);
  const mes=document.getElementById('f-mes').value.trim();
  // trava: ano, mês e nome do produto são os filtros da consulta
  if(!term||!ano||!mes){
    prodOpen=false;
    document.getElementById('prodResults').classList.remove('show');
    toast('⚠️','Preencha os filtros','Informe ano de adesão, mês de adesão e o nome do produto antes de consultar.');
    return;
  }
  prodOpen=true;
  // a API filtra por subproduto + ano de adesão; sobra escolher só a periodicidade/capital
  const list=PRODUTOS.filter(p=>p.sub.toLowerCase().includes(term.toLowerCase()) && p.ano===ano);
  renderProd(list,ano,mes);
  if(list.length) toast('⚙','Consulta ao banco de produtos', list.length+' versão(ões) para '+term+' · adesão '+ano+'. Escolha a periodicidade/capital.');
  else toast('🔎','Nenhuma versão encontrada','Não há '+term+' com adesão em '+ano+'. Confira o ano informado.');
}
function renderProd(list,ano,mes){
  const box=document.getElementById('prodResults');
  if(!list.length){box.innerHTML=`<div class="prod-item" style="cursor:default;color:var(--text-dim)">Nenhuma versão para adesão ${ano}</div>`;box.classList.add('show');return;}
  box.innerHTML=`<div class="prod-head">${list[0].sub} · adesão ${ano} — escolha a periodicidade / capital</div>`+list.map(p=>{
    return `<div class="prod-item ${selProd&&selProd.id===p.id?'sel':''}" onclick="pickProd(${p.id})">
      <div class="pi-ic">💗</div>
      <div style="min-width:0"><div class="pi-name">${p.periodicidade}</div>
      <div class="pi-meta">${p.sub} · adesão ${p.ano}/${p.mes} · cód #${p.id}</div></div>
      <div class="check">✓</div></div>`;
  }).join('');
  box.classList.add('show');
}
function pickProd(id){
  selProd=PRODUTOS.find(p=>p.id===id);
  queryProd();
  document.getElementById('f-prod').value=selProd.sub+" — "+selProd.ano;
  document.getElementById('f-period').value=selProd.periodicidade;
  document.getElementById('f-cg').textContent=selProd.cg;
  document.getElementById('prodSelected').style.display='block';
  document.getElementById('btnDisparar').disabled=false;
}

/* ===== elegibilidade ===== */
function checkElig(motivo){
  const m=(motivo||'').toLowerCase();
  if(m.includes('suicíd')||m.includes('suicid')) return {ok:false,regra:'CG · Cláusula 5.1 (a)',txt:'Suicídio nos primeiros 24 meses de vigência está expressamente excluído da cobertura (carência legal).'};
  if(m.includes('preexist')) return {ok:false,regra:'CG · Cláusula 5.2',txt:'Doença preexistente não declarada na contratação está fora da cobertura.'};
  if(m.includes('ilícit')||m.includes('ilicit')) return {ok:false,regra:'CG · Cláusula 5.4',txt:'Eventos decorrentes de ato ilícito do segurado estão excluídos.'};
  return {ok:true};
}

/* ===== disparar ===== */
function dispararAnalise(){
  caseData={id:document.getElementById('f-id').value||'sinistro-novo',
    prod:selProd?selProd.sub+" ("+selProd.ano+" · "+(selProd.maior30?"+30 Mil":selProd.periodicidade.includes('até 30')?"até 30 Mil":"")+")":'—',
    motivo:document.getElementById('f-motivo').value||'—',
    ano:document.getElementById('f-ano').value,mes:document.getElementById('f-mes').value,
    maior30:selProd?selProd.maior30:false, cg:selProd?selProd.cg:'', reg:selProd?selProd.reg:''};
  caseData.herdeiros=false;
  eligState=checkElig(caseData.motivo); overridden=false; received=[];
  CL = eligState.ok ? buildChecklist({maior30:caseData.maior30}) : [];
  loadCase();
  if(eligState.ok) toast('✦','Análise disparada','Caso elegível — checklist montado a partir da CG '+caseData.cg+'.');
  else toast('⚠️','Possível não cobertura','O agente não gerou o checklist automaticamente. Veja o motivo no caso.');
  show('view-case');
}

/* ===== abrir caso exemplo ===== */
function openCase(kind){
  kind=kind||'default';
  selProd=PRODUTOS.find(p=>p.id===2024); // Vida Mulher 2020 +30
  caseData={prod:'Vida Mulher (2020 · +30 Mil)',ano:'2020',mes:'Dezembro',maior30:true,herdeiros:false,cg:selProd.cg,reg:selProd.reg};
  overridden=false; received=[];

  if(kind==='naoelegivel'){
    caseData.id='sinistro-2026-04830'; caseData.motivo='Suicídio dentro do período de carência';
    eligState=checkElig(caseData.motivo); CL=[];
    loadCase(); show('view-case'); return;
  }

  eligState={ok:true};
  if(kind==='herdeiros'){
    caseData.id='sinistro-2026-04825'; caseData.motivo='Morte natural · sem beneficiário indicado (herdeiros legais)';
    caseData.herdeiros=true;
    CL=buildChecklist({maior30:true,herdeiros:true});
    setItem(0,'validado');  // Certidão de óbito
    setItem(1,'validado');  // RG do segurado
    setItem(CL.findIndex(c=>c.div&&c.pessoa),'diverg'); // RG do 1º filho diverge
  } else {
    caseData.id='sinistro-2026-04817'; caseData.motivo='Morte natural do titular';
    CL=buildChecklist({maior30:true,herdeiros:false});
    setItem(0,'validado');  // Certidão de óbito
    setItem(1,'diverg');    // RG do segurado diverge
  }
  loadCase(); show('view-case');
}
function setItem(i,st){
  if(i<0) return;
  CL[i].st=st; CL[i].doc=CL[i].file;
  CL[i].valid = (st==='diverg'&&CL[i].div) ? {result:'diverg',d:CL[i].div} : {result:'ok'};
  received.push({nm:CL[i].file,tipo:CL[i].nm,flag:st==='diverg'});
}

/* ===== render caso ===== */
function loadCase(){
  document.getElementById('c-id').textContent=caseData.id;
  document.getElementById('c-prod').textContent=caseData.prod;
  document.getElementById('c-motivo').textContent=caseData.motivo;
  document.getElementById('c-adesao').textContent=caseData.mes+'/'+caseData.ano;
  renderElig(); renderChecklist(); renderDocs(); renderCG(); renderStatus();
}

function renderElig(){
  const box=document.getElementById('eligBox');
  if(eligState.ok){
    box.innerHTML=`<div class="elig ok"><div class="eic">✅</div><div>
      <div class="et">Elegível — evento coberto</div>
      <div class="es">Nenhuma exclusão identificada para o motivo reportado. Checklist gerado automaticamente.</div></div></div>`;
  } else if(overridden){
    box.innerHTML=`<div class="elig no"><div class="eic">⚖️</div><div>
      <div class="et">Prosseguindo sob ressalva</div>
      <div class="es">Indício de não cobertura por <b>${eligState.regra}</b>, mas a regulação segue por decisão do regulador. ${eligState.txt}</div></div></div>`;
  } else {
    box.innerHTML=`<div class="elig no"><div class="eic">🛑</div><div>
      <div class="et">Possível não cobertura — checklist não gerado</div>
      <div class="es">${eligState.txt}<br>Base: <b>${eligState.regra}</b>.</div>
      <div class="elig-actions">
        <button class="btn btn-warn sm" onclick="prosseguir()">⚖️ Prosseguir mesmo assim</button>
        <button class="btn btn-ghost sm" onclick="goHome()">Encerrar sem regular</button>
      </div></div></div>`;
  }
}
function prosseguir(){
  overridden=true; CL=buildChecklist({maior30:caseData.maior30,herdeiros:caseData.herdeiros}); received=[];
  loadCase();
  toast('⚖️','Regulação retomada','Checklist gerado por decisão do regulador, apesar do indício de não cobertura.');
}

function renderChecklist(){
  const area=document.getElementById('checklistArea');
  const blocked = !eligState.ok && !overridden;
  area.style.display = blocked ? 'none':'block';
  document.getElementById('eligBox').nextElementSibling; // noop
  if(blocked) return;

  // banners de regras/OT aplicadas
  let banners='';
  if(caseData.herdeiros) banners+=`<div class="ot-banner"><div class="ic">⚖️</div><div class="txt"><b>OT-2024-11 aplicada.</b> Sem beneficiário indicado na apólice — a indenização segue para os <b>herdeiros legais</b>, exigindo documento de identificação dos herdeiros e Formal de Partilha / Alvará Judicial.</div></div>`;
  if(caseData.maior30) banners+=`<div class="ot-banner"><div class="ic">⚠️</div><div class="txt"><b>OT-2025-07 aplicada.</b> Capital segurado acima de R$ 30.000 — o <b>Termo de Quitação e Outorga</b> passou a ser obrigatório no checklist (firma reconhecida).</div></div>`;
  document.getElementById('ot-banner').innerHTML = banners;

  const ul=document.getElementById('checklist');
  ul.innerHTML=CL.map((it,i)=>{
    let icon = it.st==='validado'?'✓': it.st==='analise'?'<span class="spin"></span>': it.st==='diverg'?'!':'';
    let tag = it.origem.t==='ot'?`<span class="tag ot">⚠ Exigido por ${it.origem.ref}</span>`:`<span class="tag cg">📑 ${it.origem.ref}</span>`;
    let extra='';
    if(it.st==='validado'){
      extra=`<div class="docref">✓ Validado · <a onclick="dl('${it.doc}')">${it.doc}</a></div>`;
      if(it.valid&&it.valid.ressalva) extra+=`<div class="ressalva">⚠ Aceito com ressalva pelo regulador</div>`;
    }
    if(it.st==='analise') extra=`<span class="pill amber" style="margin-top:8px;display:inline-flex"><span class="dot"></span>Analisando…</span>`;
    if(it.st==='diverg'){
      const d=it.valid.d;
      extra=`<div class="diverg-box"><div class="db-t">⚠ Divergência na validação cruzada</div>
        <div class="cmp">
          <div class="col"><div class="lb">No documento</div><div class="vl bad">${d.doc}</div></div>
          <div class="col"><div class="lb">No cadastro do sinistro</div><div class="vl">${d.cad}</div></div>
        </div>
        <div class="mini-btns">
          <button class="mini-btn ok" onclick="resolver(${i},'aceitar')">Aceitar com ressalva</button>
          <button class="mini-btn re" onclick="resolver(${i},'reenvio')">Solicitar reenvio</button>
        </div></div>`;
    }
    let subline = it.sub?`<div style="font-size:11px;color:var(--text-dim);margin-top:2px">👤 ${it.sub}</div>`:'';
    return `<li class="ci ${it.st}"><div class="stat ${it.st}">${icon}</div>
      <div class="body"><div class="nm">${it.nm}</div>${subline}<div class="why">${tag}</div>${extra}</div></li>`;
  }).join('');

  const total=CL.length, val=CL.filter(c=>c.st==='validado').length, div=CL.filter(c=>c.st==='diverg').length;
  document.getElementById('cl-count').textContent=val+'/'+total;
  document.getElementById('cl-fill').style.width=(total?val/total*100:0)+'%';
  document.getElementById('cl-txt').textContent=val+' de '+total+' validados'+(div?' · '+div+' com divergência':'');
}

function resolver(i,acao){
  if(acao==='aceitar'){
    CL[i].st='validado'; CL[i].valid={result:'ok',ressalva:true};
    const r=received.find(d=>d.tipo===CL[i].nm); if(r)r.flag=false;
    toast('✓','Divergência aceita com ressalva','O item foi validado por decisão do regulador. A ressalva fica registrada no processo.');
  } else {
    CL[i].st='pendente'; CL[i].doc=null; CL[i].valid=null;
    received=received.filter(d=>d.tipo!==CL[i].nm);
    toast('↩️','Reenvio solicitado','Documento removido do caso. Solicitação de novo envio registrada para o beneficiário.');
  }
  renderChecklist(); renderDocs(); renderStatus();
}

function renderDocs(){
  const ul=document.getElementById('docList');
  if(!received.length){ul.innerHTML='<li class="empty">Nenhum documento recebido ainda.</li>';return;}
  ul.innerHTML=received.map(d=>`<li class="doc ${d.flag?'flag':''}">
    <div class="fic">📄</div>
    <div style="min-width:0"><div class="dn">${d.nm}</div><div class="dm">Identificado: ${d.tipo}</div></div>
    ${d.flag?'<span class="warn" title="Divergência na validação cruzada">⚠</span>':''}
    <button class="dl" onclick="dl('${d.nm}')" title="Baixar">↓</button></li>`).join('');
}
function renderCG(){
  document.getElementById('cgList').innerHTML=`
    <li class="doc"><div class="fic">📕</div><div style="min-width:0"><div class="dn">${caseData.cg}</div><div class="dm">Condições Gerais · adesão ${caseData.ano}</div></div><button class="dl" onclick="dl('${caseData.cg}')" title="Baixar">↓</button></li>
    <li class="doc"><div class="fic">📙</div><div style="min-width:0"><div class="dn">${caseData.reg}</div><div class="dm">Regulamento da promoção</div></div><button class="dl" onclick="dl('${caseData.reg}')" title="Baixar">↓</button></li>
    ${caseData.maior30?`<li class="doc"><div class="fic">📒</div><div style="min-width:0"><div class="dn">OT-2025-07_Termo_Quitacao.pdf</div><div class="dm">Orientação Técnica aplicada</div></div><button class="dl" onclick="dl('OT-2025-07')" title="Baixar">↓</button></li>`:''}`;
}
function renderStatus(){
  const el=document.getElementById('c-status');
  if(!eligState.ok&&!overridden){el.innerHTML='<span class="pill red"><span class="dot"></span>Não elegível</span>';return;}
  const total=CL.length, val=CL.filter(c=>c.st==='validado').length, div=CL.filter(c=>c.st==='diverg').length, pend=CL.filter(c=>c.st==='pendente'||c.st==='analise').length;
  let html='';
  if(div>0) html='<span class="pill amber"><span class="dot"></span>Divergências a resolver</span>';
  else if(pend>0) html='<span class="pill blue"><span class="dot"></span>Documentação pendente</span>';
  else html='<span class="pill green"><span class="dot"></span>Documentação completa</span>';
  if(overridden) html='<span class="pill red"><span class="dot"></span>Sob ressalva</span> '+html;
  el.innerHTML=html;
}

/* ===== receber documento (auto-classificação + validação cruzada) ===== */
function receberDoc(){
  if(!eligState.ok&&!overridden){toast('🛑','Caso não elegível','Prossiga sob ressalva para habilitar o recebimento de documentos.');return;}
  const idx=CL.findIndex(c=>c.st==='pendente');
  if(idx<0){toast('✅','Nada pendente','Todos os documentos exigidos já foram recebidos.');return;}
  CL[idx].st='analise'; renderChecklist();
  toast('🔍','Analisando documento…','Identificando tipo e cruzando com o cadastro do sinistro.');
  setTimeout(()=>{
    const conf=92+Math.floor(Math.random()*7);
    if(CL[idx].div){
      CL[idx].st='diverg'; CL[idx].doc=CL[idx].file; CL[idx].valid={result:'diverg',d:CL[idx].div};
      received.push({nm:CL[idx].file,tipo:CL[idx].nm,flag:true});
      toast('⚠️','Divergência: '+CL[idx].nm, 'Dado do documento não bate com o cadastro do sinistro. Veja o detalhe no checklist.');
    } else {
      CL[idx].st='validado'; CL[idx].doc=CL[idx].file; CL[idx].valid={result:'ok'};
      received.push({nm:CL[idx].file,tipo:CL[idx].nm,flag:false});
      toast('✓','Documento validado: '+CL[idx].nm, conf+'% de confiança · validação cruzada OK.');
    }
    renderChecklist(); renderDocs(); renderStatus();
  },1100);
}

function dl(n){toast('📥','Download iniciado',n)}

/* ===== toast ===== */
function toast(icon,title,sub){
  const c=document.getElementById('toasts');const t=document.createElement('div');t.className='toast';
  t.innerHTML=`<div class="ti">${icon}</div><div><div class="tt">${title}</div>${sub?`<div class="ts">${sub}</div>`:''}</div>`;
  c.appendChild(t);setTimeout(()=>{t.style.animation='slideOut .3s forwards';setTimeout(()=>t.remove(),300)},3800);
}
</script>
</body>
</html>
